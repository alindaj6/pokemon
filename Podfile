# Uncomment the next line to define a global platform for your project
platform :ios, '17.0'

target 'Pokemon' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Pokemon
  pod 'Moya', '15.0.0' # required to do network request
  
  pod 'SnapKit', '5.6.0'

  # Network Image
  pod 'Kingfisher', '7.10.1'

  pod 'SwiftMessages', '~> 9.0.9'

  target 'PokemonTests' do
    inherit! :search_paths
    # Pods for testing
    # Pods for testing
    pod 'Quick', '~> 5.0.0'
    pod 'Nimble', '~> 10.0.0'
  end

  target 'PokemonUITests' do
    # Pods for testing
  end

end
